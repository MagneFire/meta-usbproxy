# usb-proxy appliance kernel config fragment (Orange Pi Zero / sunxi musb).
# Everything the device side of usb-proxy needs, built-in (=y) so it is present
# at boot with no module loading.

# raw-gadget: the UDC interface usb-proxy drives (/dev/raw-gadget).
CONFIG_USB_GADGET=y
CONFIG_USB_RAW_GADGET=y

# sunxi musb peripheral controller + its USB PHY.
#
# Build musb PERIPHERAL-ONLY (not dual-role): on this appliance the micro-USB
# OTG port is only ever the gadget to the upstream host, and the devices being
# proxied attach to the separate EHCI/OHCI USB-A ports. In dual-role the OTG
# ID pin on the Orange Pi Zero reads as "host" (extcon USB-HOST asserted), so
# musb switches to host mode and never enumerates as a device. Peripheral-only
# disables that role-detection and keeps it a gadget.
CONFIG_USB_MUSB_HDRC=y
CONFIG_USB_MUSB_SUNXI=y
CONFIG_USB_MUSB_GADGET=y
# CONFIG_USB_MUSB_DUAL_ROLE is not set
# CONFIG_USB_MUSB_HOST is not set
CONFIG_NOP_USB_XCEIV=y
CONFIG_PHY_SUN4I_USB=y

# musbfix is obsolete here: the RX-requeue fix is patched directly into
# drivers/usb/musb/musb_gadget.c, so KPROBES/FTRACE are no longer required.
